# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
build/
.next/
out/
# Cliente de Prisma generado (se regenera con `prisma generate`)
generated/

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# TypeScript
*.tsbuildinfo

# Cache
.cache/
.eslintcache

# Cobertura de pruebas (generada por jest)
coverage/

# autoskills: skills locales de terceros, no se versionan
skills-lock.json

# AI tooling
.agents/
.claude/
CLAUDE.md

# Documentación: las notas en Markdown de docs/ SÍ se versionan.
# docs/fuentes/ guarda los originales pesados (docx, xlsx, pdf, esquemas) que
# NO se suben al repo.
docs/fuentes/
# Temporales de Office (Word deja estos archivos mientras un .docx está abierto).
~$*

# Herramientas personales (no se versionan)
# Colección de Postman para probar la API y guía personal del backend.
postman/
docs/guia-backend.html

# Documentos nuevos de docs/ que NO se suben (generados / de trabajo).
# Nota: los .md ya versionados (MER.md, flujo-completo.md, etc.) siguen en git.
docs/consumo-api-frontend.docx
docs/rutas-postman.docx
docs/guia-tecnologias-backend.html
docs/despliegue-railway.md

# Módulo descartado
avisens-api-gateway/
