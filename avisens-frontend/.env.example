# Copia este archivo como .env.local y completa los valores reales.
# .env.local no se sube a git (ver .gitignore) — cada quien pone los suyos.

VITE_API_URL=/api

# Firebase — solo para las lecturas en vivo de sensores (Realtime Database).
# Se obtienen en Firebase Console → Configuración del proyecto → Tus apps.
