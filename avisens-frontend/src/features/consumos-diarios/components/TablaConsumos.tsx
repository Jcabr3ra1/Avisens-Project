import type { ConsumoDiario } from '../model/consumoDiario'
type Props = { consumos: ConsumoDiario[]; onEditar: (consumo: ConsumoDiario) => void; onEliminar: (consumo: ConsumoDiario) => void }
function TablaConsumos({ consumos, onEditar, onEliminar }: Props) { return <div className="cd-scroll"><table><thead><tr><th>Fecha</th><th>Lote</th><th>Alimento</th><th>Consumo</th><th>Agua</th><th>Acciones</th></tr></thead><tbody>{consumos.map((c) => <tr key={c.id}><td>{new Date(`${c.fecha.slice(0, 10)}T12:00:00`).toLocaleDateString('es-CO')}</td><td><b>{c.lote.codigo}</b></td><td>{c.tipo_alimento?.nombre ?? 'Sin alimento'}</td><td>{c.alimento_kg === null ? '—' : `${Number(c.alimento_kg).toLocaleString('es-CO')} kg`}</td><td>{c.agua_litros === null ? '—' : `${Number(c.agua_litros).toLocaleString('es-CO')} L`}</td><td><div><button type="button" onClick={() => onEditar(c)}>Editar</button><button className="cd-borrar" type="button" onClick={() => onEliminar(c)}>Eliminar</button></div></td></tr>)}</tbody></table></div> }
export default TablaConsumos
